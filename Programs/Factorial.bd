big main() << 0
	normal nvar << 9,	
	big fact << Factorial(nvar),	
	print fact,
	return 0.
	
	
big Factorial(normal nvar << 0) << 0
	big result << 1,
	normal multiplyer << 1,
	until nvar do
		result << result * multiplyer,
		multiplyer << multiplyer + 1,
		nvar << nvar - 1.
	,
	return result.